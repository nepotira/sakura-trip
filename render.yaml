services:
  - type: web
    name: sakura-trip
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python server.py"
    plan: free
    healthCheckPath: /api/config/key
    envVars:
      - key: GEMINI_API_KEY
        sync: false
